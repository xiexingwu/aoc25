# System Dependencies
- Zig 0.15.2

# Quick start
Navigate to a folder:
```bash
cd d1 # or any other day
```
Run test cases (including the sample input files)
```bash
zig test src/main.c
```
Run the code for the input file:
```bash
zig build run
```
